# 1.1.0（构建 5）验收记录

日期：2026-09-12。环境：macOS 26.5.2（25F84），Apple Silicon。
审查基线：`6e65015`；审查对象为本次工作区改动及新增文件。

## 自动回归

执行：

```bash
LIBRESHOT_CHECK_SELECTION=/tmp/LibreShot-release-qa \
LIBRESHOT_QA_OUTPUT=/tmp/LibreShot-release-qa \
tests/run-toolbar-checks.sh
```

| 项目 | 结果 |
| --- | --- |
| 窗口及内联文字编辑器按一次 Esc 取消 | 通过 |
| 系统 Vision 识别真实生成的中英文图片 | 通过 |
| 翻译的同语言返回路径 | 通过 |
| 英译中结果 | 未执行：本机未安装对应语言模型 |
| 采集配置排除指针，保持物理分辨率 | 通过 |
| 权限中文说明、首次授权成功继续、保留底层错误 | 通过 |
| 全工具八向缩放、边界和最小尺寸、拖动优先级、保留标注（72 组） | 通过 |
| 实际 OverlayView 在框选结束即渲染四角控制点 | 通过 |
| 工具显示、必选项、排序、持久化、异常旧配置和恢复默认 | 通过 |
| 截图配置快照、默认工具及长截图入口条件 | 通过 |
| 五种完成操作包含未提交文字 | 通过 |
| 工具栏换行、屏幕边缘及提示边界（144 种配置） | 通过 |
| 系统图标及原生设置、工具栏渲染 | 通过 |
| 自动保存开关、可读 PNG、保存失败保留剪贴板 | 通过 |

测试使用独立偏好域、临时目录与独立剪贴板。

## 本机界面检查

正常签名的 Debug 构建中已验证：设置菜单、⌘逗号和重新打开应用进入设置；工具排序重启后保留且下一次截图采用新顺序；工具提示可见；框选后不用 Esc 即可拖动右下角放大、文字工具下左上角缩小，随后绘制矩形仍正常。默认工具、文字输入和颜色面板打开时，一次 Esc 均可取消。四色入口和颜色选择已检查。

长截图启动后观察到“已采集 1 帧”的状态，但未完成可信的多帧拼接结果验证。随后界面自动化工具自身返回 ScreenCaptureKit `-3811`（无法开始流播放）；这不足以判定 LibreShot 的拼接逻辑失败，也不足以将长截图记为通过。需用 `tests/fixtures/capture-check.html` 手工滚动，检查编号连续、无重复或缺失，并保存 PNG 验证尺寸。

未覆盖：其他 macOS 版本、Intel 实机、跨语言翻译结果、最终导出 PNG 的人工鼠标残影检查。自动化已验证指针采集配置和 PNG 输出，但不能代替这些实测。

## 两项独立代码审查

**Standards（代码规范）**：未发现确定的规范违规。审查建议补充颜色面板下 Esc 的验证，并修正测试中误导性的 `afterEscape` 命名；已完成界面验证并改为 `selectionMode`。

**Spec（用户需求）**：未发现确定的需求偏离或范围扩张。自动选中已有标注没有混入本版。冻结预览失败后的原有回退路径不属于本次新增逻辑；长截图和翻译仍按上述验收边界记录。

## 性能与构建

本机未优化检查构建：18 项顺序配置的二进制 plist 为 222 字节，可见性过滤和布局计算 100,000 次的单次均值约 7–13 微秒。该测量不代表相对旧版净增耗时，也不代表进程内存峰值。排序不新增轮询、定时器或图像缓冲区。

签名 Release archive 构建成功，包含 arm64 和 x86_64。打包脚本验证应用签名、DMG 完整性并生成 SHA256SUMS，输出到新的 `dist/` 子目录，保留历史 `build/` 文件。

安装验收：Release 已替换 `/Applications/LibreShot.app`，启动后“关于”显示 1.1.0；安装后签名和 DMG 校验通过。原安装版元数据为 1.0.2，已备份到 `dist/backups/LibreShot-before-1.1.0.app`。工具栏取消项仍排第一、模糊仍隐藏，保存目录及自动保存设置保留。

构建仍有现存警告，包括 SwiftUI 弃用接口、冗余 try/await、Translation 状态的未来枚举分支、应用分类和图标尺寸。当前编译模式下不阻断构建；尚不能宣称 Swift 6 严格模式兼容。
