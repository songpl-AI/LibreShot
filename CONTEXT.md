# CONTEXT

LibreShot 的领域词汇表。只收录概念与术语，不含实现细节。

## 截图会话（Capture Session）
一次区域截图或长截图的完整生命周期：从遮罩层出现到最终出图（复制/保存/贴图/OCR）。

## 选区（Selection Rect）
用户在遮罩层上框选的目标区域，是裁剪与标注的边界。可移动、可缩放（八向把手）。

## 标注（Annotation）
叠加在选区上的一个绘图元素。类型：画笔、矩形、箭头、椭圆、文字、马赛克、模糊、序号。

## 选择模式（Select Mode）
无标注工具激活的状态：可移动/缩放选区、选中与拖动已有标注，不能新建标注。

## 序号标注（Numbered Annotation）
带圆圈、按创建顺序自动递增的数字标记，用于指引截图中的关注点。

## 标注颜色（Annotation Color）
标注的显示颜色，工具栏可选（预设色板 + 系统取色器），作用于画笔、文字、序号等所有标注类型。

## 字号（Font Size）
文字/序号标注的字体大小。通过拖动选中文字的缩放手柄等比缩放，或工具栏下拉精确设置。

## 自动保存（Auto-save）
出图后直接写入预设目录，不弹出保存面板。

## 另存为（Save As）
手动经保存面板指定位置的保存，与自动保存互为补充。

## OCR 结果（OCR Result）
对截图识别出的文本，在独立窗口只读展示。

## 翻译（Translation）
对 OCR 文本的设备端离线翻译（系统 `Translation` 框架）。
